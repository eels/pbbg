import %name from 'components/%type/%name';
import type { %nameProps } from 'components/%type/%name';
import type { Story } from '@storybook/react';

export default {
  component: %name,
  title: 'Components/%!type/%name',
};

export const Default: Story<%nameProps> = (args) => <%name {...args} />;

Default.args = {};
