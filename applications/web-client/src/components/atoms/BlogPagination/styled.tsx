import styles from 'components/atoms/BlogPagination/blog-pagination.module.scss';
import { create } from 'chic-modules';

const styled = create(styles);

export const Wrapper = styled.div('wrapper');
