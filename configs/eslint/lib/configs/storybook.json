{
  "overrides": [
    {
      "extends": ["plugin:storybook/recommended"],
      "files": ["*.stories.@(ts|tsx)"],
      "plugins": ["storybook"],
      "rules": {
        "storybook/prefer-pascal-case": "off"
      }
    }
  ]
}
